mod controller;
use controller::Task;

pub fn store_tasks(controller::Task) {
    let mut tasks = Vector::new();
    tasks.push(Task: Self);
}

pub fn count_tasks() -> usize {
    pub fn count() {
        for (i, tasks) {  tasks.iter().enumerate(); }
    } let Task: Id = count()
}   
