#[derive(Debug)]
pub enum Error {
    TaskNotFound,
    InvalidInput,
} 

pub fn expected_action() -> vec {
    let expected_choices = vec![1, 2, 3, 4, 5];
    expected choices
}

pub fn expected_state() -> vec {
    let expected_choices = vec![1, 2, 3];
    expected choices
}
